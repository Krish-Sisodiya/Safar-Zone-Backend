spring.application.name=safar-zone-backend

# Server Configuration
server.port=8080

# MySQL Database (Production Secure Config)
spring.datasource.url=jdbc:mysql://${DB_HOST:mysql-15744e82-krishsisodiya7389-c60d.j.aivencloud.com}:${DB_PORT:20026}/${DB_NAME:defaultdb}?sslMode=REQUIRED&allowPublicKeyRetrieval=true&connectTimeout=30000&socketTimeout=30000
spring.datasource.username=${DB_USER:avnadmin}
spring.datasource.password=${DB_PASSWORD:AVNS_DgWAubd6dwv_ctmTNhY}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=Asia/Kolkata

# JWT Secret (Production environment variable)
app.jwt.secret=${JWT_SECRET:SafarZoneSecretKeyMustBeAtLeast256BitsLongForHS256!@#}
app.jwt.expiration-ms=86400000

# ===== FILE UPLOAD CONFIG =====
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
# Temporary root folder for cloud deployment
app.upload-dir=${UPLOAD_DIR:/tmp/safar-uploads}

# Swagger/OpenAPI Config
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.display-request-duration=true

# =====================================================
# BREVO CLOUD REST BYPASS
# =====================================================
# Property ka naam alag kar diya taaki circular dependency na bane
app.brevo.api-key=${BREVO_API_KEY:no_key_found}
app.brevo.sender-email=${MAIL_USERNAME:masterkrish812@gmail.com}
# =====================================================
# RAZORPAY
# =====================================================
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_StCIifU5u85oRJ}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:Hxy3m53ypESmOWx4CzRgtIWq}

# CORS (Allow Expo/React Native)
# CORS (Allow Expo / Web / Wildcard)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}

logging.level.org.springframework.web.cors=DEBUG
logging.level.com.safar_zone_backend.config.CorsConfig=DEBUG